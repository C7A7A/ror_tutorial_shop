= form_with(model: line_item, local: true) do |form|
  - if line_item.errors.any?
    #error_explanation
      %h2 #{pluralize(line_item.errors.count, 'error')} prohibited this line_item from being saved

      %ul
        - line_item.errors.full_messages do |message|
          %li= message
  
  .field
    = form.label :product_id
    = form.text_field :product_id

  .field
    = form.label :cart_id
    = form.text_field :cart_id

  .actions
    = form.submit